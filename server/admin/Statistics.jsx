import React, { useState, useEffect } from 'react'
import {Section, Box, H1, TableRow, TableCell} from '@adminjs/design-system'
import getSoldProducts from '../service/statistic/getSoldProducts'



const Statistics = (props) => {

    // const [token, setToken] = useState(null);
    //
    // console.log('fgsdfds')
    //
    // useEffect(() => {
    //     (async function getToken() {
    //         const token = await getSoldProducts()
    //         setToken(token);
    //     })()
    // }, [])
    //
    // console.log(token)

    return (
        <Box>
        <H1>hiljhlkh</H1>
        </Box>)
}

export default Statistics